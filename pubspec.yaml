name: place_pickarte
description: Pixel-by-pixel customizable map place picker for Flutter.
version: 1.0.2
homepage: https://kamranbekirov.com
repository: https://github.com/kamranbekirovyz/place-pickarte

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.17.1
  google_maps_flutter_ios_sdk10: ^2.18.4
  rxdart: ^0.28.0
  geolocator: ^14.0.2
  http: ^1.6.0
  json_annotation: ^4.12.0
  meta: ^1.18.0
  latlong2: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  json_serializable: ^6.14.0

flutter: